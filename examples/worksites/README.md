# Worksite examples

`WO-014.invalid.example.json` is deliberately non-operational. Its SFace
descriptors contain only three values so validation and error-reporting flows
can be demonstrated. Production SFace descriptors must contain exactly 128
finite numeric values and have a nonzero norm.

Do not copy this invalid synthetic example into `config/worksites/` for deployment.
